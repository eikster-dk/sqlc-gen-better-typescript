version: '2'
plugins:
- name: effect
  wasm:
    url: file://../../dist/plugin
sql:
- schema: schema/
  queries: queries/
  engine: postgresql
  codegen:
  - out: src/repositories
    plugin: effect
    options: 
      builder: effect-v4-unstable
      import_extension: ".js"
      # disable_template_literals: true
      # debug: true
      # debug_dir: debug
