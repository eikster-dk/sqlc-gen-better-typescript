version: '2'
plugins:
- name: effect
  wasm:
    url: file://../../dist/plugin
sql:
- schema: schema/schema.sql
  queries: schema/queries.sql
  engine: postgresql
  codegen:
  - out: src/models
    plugin: effect
    options: 
      builder: effect-v4-unstable
      debug: true
      debug_dir: debug
