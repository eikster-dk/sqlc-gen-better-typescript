version: '2'
plugins:
- name: effect
  wasm:
    url: file://../../dist/sqlc-gen-effect.wasm
sql:
- schema: schema/
  queries: queries/
  engine: postgresql
  codegen:
  - out: src/repositories
    plugin: effect
    options: 
      import_extension: ".js"
      # disable_template_literals: true
      # debug: true
      # debug_dir: debug
