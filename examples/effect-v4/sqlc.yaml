version: '2'
plugins:
- name: effect
  process:
      cmd: ../../dist/plugin
sql:
- schema: schema/schema.sql
  queries: schema/queries.sql
  engine: postgresql
  codegen:
  - out: src/models
    plugin: effect
    options: {}
