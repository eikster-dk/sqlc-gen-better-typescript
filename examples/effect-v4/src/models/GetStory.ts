// Query: GetStory
// Command: :one
// SQL: select id, name from sms_stories where id = $1

// Parameters:
//   id: Schema.String (position 1)

// Results:
//   id: Schema.String
//   name: Schema.String
