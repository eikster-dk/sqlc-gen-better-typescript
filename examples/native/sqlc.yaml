version: '2'
plugins:
- name: native
  wasm:
    url: file://../../dist/sqlc-gen-native.wasm
sql:
- schema: schema/
  queries: queries/
  engine: postgresql
  codegen:
  - out: src
    plugin: native
    options:
      import_extension: ".js"
