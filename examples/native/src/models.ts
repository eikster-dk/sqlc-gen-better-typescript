// Generated by sqlc v1.31.1, plugin: sqlc-effect dev
import { z, ZodError } from "zod"

export const OrderStatus = z.union([z.literal("pending"), z.literal("confirmed"), z.literal("processing"), z.literal("shipped"), z.literal("delivered"), z.literal("cancelled"), z.literal("refunded")])
export type OrderStatus = z.infer<typeof OrderStatus>

export const CustomersRow = z.object({
  id: z.number(),
  email: z.string(),
  name: z.string(),
  phone: z.string().nullable(),
  created_at: z.coerce.date(),
  updated_at: z.coerce.date(),
})
export type CustomersRow = z.infer<typeof CustomersRow>

export const OrdersRow = z.object({
  id: z.number(),
  customer_id: z.number(),
  status: OrderStatus,
  total_cents: z.number(),
  shipping_address: z.string().nullable(),
  billing_address: z.string().nullable(),
  notes: z.string().nullable(),
  created_at: z.coerce.date(),
  updated_at: z.coerce.date(),
  search_vector: z.unknown().nullable(),
})
export type OrdersRow = z.infer<typeof OrdersRow>

export interface SqlQueryResult {
  rows: unknown[]
  rowCount?: number | null
  command?: string
}

export interface SqlClient {
  query(queryText: string, values?: unknown[]): Promise<SqlQueryResult>
}

export interface SqlExecResult {
  command: string
  rowCount: number
}

export type QueryResult<T> =
  | { success: true; data: T }
  | { success: false; error: ZodError; phase: "input" | "output" }
